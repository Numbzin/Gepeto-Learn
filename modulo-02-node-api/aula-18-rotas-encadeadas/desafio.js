/**
 * Aula 18 (Refatorada) – Rota encadeada com parâmetro
 *
 * Desafio:
 * - Criar a rota GET /usuarios/:id/sombras
 * - Usar req.params.id para filtrar as sombras no JSON
 * - Retornar apenas as sombras que pertencem ao usuário informado
 * - Modularizar com controller separado (listarSombrasDoUsuario)
 */
