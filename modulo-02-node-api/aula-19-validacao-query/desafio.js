/**
 * Aula 19 — Validação de Entrada (Desafio real)
 *
 * Objetivo:
 * Criar rotas protegidas por middlewares de validação
 *
 * Tarefas:
 * - Criar o middleware validarIdUsuario para rota /usuarios/:id/sombras
 * - Criar o middleware validarNivelQuery para rota /sombras?nivel=...
 * - Criar o controller listarSombrasDoUsuario que filtra sombras por usuarioId
 *
 * NADA está pronto — você vai montar tudo com base no que já aprendeu.
 */
